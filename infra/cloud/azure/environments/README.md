# Azure Environments

Environment overlays for Azure deployment.

```text
environments/
  dev/
  staging/
  prod/
```

Each environment folder should contain only environment-specific inputs (for example, variable files, parameters, and overrides).
